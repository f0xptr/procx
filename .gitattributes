# .gitattributes
# Configure Git attributes for path-specific settings.

# Enforce Unix-style line endings for all text files to ensure consistency across platforms.
# This prevents issues with different line ending conventions (CRLF vs LF).
* text=auto eol=lf

# Treat specific file types as binary to prevent Git from trying to auto-convert them.
# This is especially important for certain non-text files where conversion can corrupt them.
# Add more binary types as needed.
*.png binary
*.jpg binary
*.gif binary
*.webp binary
*.svg binary
*.bmp binary
*.pdf binary
*.mp3 binary
*.wav binary
*.aiff binary
*.aac binary
*.ogg binary
*.flac binary
